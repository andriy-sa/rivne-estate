{% extends "admin/app.twig" %}

{% block content %}
    {% if success %}
        <div class="alert alert-success" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            {{ success }}
        </div>
    {% endif %}
    <div class="dataTable_wrapper estates-view view-list">
        <table class="table table-striped table-bordered table-hover" id="dataTables-example">
            <thead>
            <tr>
                <th>Фото</th>
                <th>ПІП</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for item in rieltors %}
                <tr>
                    <td>
                        {% if item.photo %}
                            <img src="/assets/userfiles/rieltors/{{ item.photo }}" alt="{{ item.name }}">
                        {% endif %}
                    </td>
                    <td>
                        {{ item.name }}
                    </td>
                    <td>
                        <a href="/admin/rieltors/update/{{ item.id }}" class="btn btn-info">
                            <i class="fa fa-pencil-square-o"></i>
                        </a>
                        <a href="/admin/rieltors/delete/{{ item.id }}" class="btn btn-danger">
                            <i class="fa fa-trash"></i>
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

{% block styles %}
    <link href="/assets/bower_components/datatables-plugins/integration/bootstrap/3/dataTables.bootstrap.css" rel="stylesheet">
{% endblock %}

{% block scripts %}
    <script src="/assets/bower_components/datatables/media/js/jquery.dataTables.min.js"></script>
    <script src="/assets/bower_components/datatables-plugins/integration/bootstrap/3/dataTables.bootstrap.min.js"></script>

    <script>
        $(document).ready(function() {
            $('#dataTables-example').DataTable({
                responsive: true
            });
        });
    </script>
{% endblock %}